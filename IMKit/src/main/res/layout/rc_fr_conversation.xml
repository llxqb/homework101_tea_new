<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/rc_normal_bg">

    <include
        android:id="@id/rc_layout_msg_list"
        layout="@layout/rc_fr_messagelist"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/end_counselling_layout_rl" />

    <io.rong.imkit.RongExtension
        android:id="@+id/rc_extension"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        app:RCStyle="SCE" />

    <RelativeLayout
        android:id="@+id/end_counselling_layout_rl"
        android:layout_width="match_parent"
        android:layout_height="62dp"
        android:layout_above="@+id/rc_extension"
        android:background="#EAECF9">

        <TextView
            android:id="@+id/end_counselling"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="20dp"
            android:layout_marginBottom="5dp"
            android:background="@drawable/bg_start_conselling_solid_5"
            android:paddingStart="10dp"
            android:paddingTop="4dp"
            android:paddingEnd="10dp"
            android:paddingBottom="4dp"
            android:text="结束辅导"
            android:textColor="#ffffff"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/end_counselling_message_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_marginEnd="14dp"
            android:layout_toStartOf="@id/end_counselling"
            android:gravity="start"
            android:maxLines="2"
            android:paddingStart="10dp"
            android:paddingEnd="5dp"
            android:text="辅导结束后，请主动点击结束辅导。\n结束辅导后，将上传辅导记录。"
            android:textColor="#9495A0"
            android:textSize="13sp" />
    </RelativeLayout>
</RelativeLayout>